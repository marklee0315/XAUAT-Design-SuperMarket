@{
    Layout = "_AdminLayout";
}
@{
    var gooditems = Model as List<MemberGoodMapping>;
}

<!DOCTYPE html>

<html>

<body>

    <div class="box">

        <div class="box-header">
            <div class="box-name">
                <i class="fa fa-table"></i>
                <span>购物信息统计</span>
            </div>
            <div class="box-icons">
                <a class="collapse-link">
                    <i class="fa fa-chevron-up"></i>
                </a>
                <a class="expand-link">
                    <i class="fa fa-expand"></i>
                </a>
                <a class="close-link">
                    <i class="fa fa-times"></i>
                </a>
            </div>
            <div class="no-move"></div>
        </div>

        <div class="box-content">
            <table class="table">
                <thead>
                    <tr>
                        <th>会员ID</th>
                        <th>总消费金额</th>
                        <th>总优惠金额</th>
                        <th>积分累计情况</th>
                    </tr>
                </thead>
                <thead>
                    <tr>
                        <th>10008</th>
                        <th>40</th>
                        <th>0</th>
                        <th>40</th>
                    </tr>
                </thead>

                @*<tbody>
            <tr>
                <td>@gooditems[0].Id</td>
                <td>
                    @for (var i = 0; i < gooditems.Count; i++)
                    {
                        @int sum;
                        @gooditems[i].GoodId
                    }
                </td>
                <td>@members[i].Phone</td>
                <td>@members[i].Credit</td>
            </tr>

        </tbody>*@
            </table>
        </div>

    </div>

</body>

</html>